Source provenance for this refactored package set:

1. Uploaded archive used as the exclusive working basis:
   - Azure_machine_configuration_hardening_packages_classic_qualitychecked.zip

2. In-package source materials carried forward and normalized:
   - documentation/windows_server_os_hardening_suggestions_common_table_only.xlsx
   - packages/win-server-*/Configuration.ps1
   - packages/win-server-*/build.ps1
   - packages/win-server-*/policy/*.json (used only as source material for the refactoring; not retained as per-package outputs)
   - root documentation and helper scripts from the uploaded archive

3. No portal policy variants are retained in the final package.
4. Final policy generation is based on:
   - policy-templates/deployIfNotExists.template.json
   - policy-templates/deployIfNotExists.enhanced.template.json
   - packages/win-server-*/policy-metadata.json

5. This package iteration additionally includes:
   - a cloud VM suitability review for Azure Windows VMs
   - reordered package numbering with recommended controls first
   - an audit-only package for the name `Administrator`
   - standardized package READMEs as short validation checklists

6. This package iteration intentionally removes:
   - NET-002
   - SECO-017

7. Optional controls occupy the highest numbers within the remaining categories.
